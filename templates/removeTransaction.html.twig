{# removeTransaction.html.twig #}
<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Supprimer un montant{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{asset('CSS/style.css')}}" type="text/css" />
        {% endblock %}

    </head>
    <body>
        {% block body %}

        <a href="/"><img class="imgCrossSign" src="{{asset('images/cross-sign.svg')}}"/></a>
        
        <h1>Supprimer un montant</h1>

        <div class="alert">
            {{ confirmation }}
        </div>

        {{ form_start(form) }}
            {{ form_row(form.selectcrypto) }}
            {{ form_row(form.quantity) }}

            <button type="submit">Valider</button>
        {{ form_end(form) }}
        
        {% endblock %}
    </body>
</html>
